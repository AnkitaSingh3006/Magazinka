import { CollapsableFilterPipe } from './collapsable-filter.pipe';

describe('CollapsableFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CollapsableFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
